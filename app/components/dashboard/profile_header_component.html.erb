<section class="rounded-xl bg-[var(--surface)] ring-1 ring-[var(--border)] p-6">
  <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:gap-6">
    <!-- avatar -->
    <div class="shrink-0">
      <%= avatar_tag %>
    </div>

    <!-- text -->
    <div class="flex-1">
      <h2 class="text-lg sm:text-xl font-semibold"><%= display_name %></h2>
      <p class="text-sm text-[var(--muted)]"><%= handle %></p>

      <% if bio.present? %>
        <p class="mt-3 text-sm leading-relaxed">
          <%= bio %>
        </p>
      <% end %>

      <div class="mt-3 flex flex-wrap gap-x-4 gap-y-2 text-xs text-[var(--muted)]">
          <span>📍Luxembourg</span>

        <% if website.present? %>
          <span>
            🔗
            <%= link_to website_short,
                        website,
                        class: "underline underline-offset-2 hover:text-[var(--link-hover)] break-all",
                        target: "_blank", rel: "noopener" %>
          </span>
        <% end %>
      </div>
    </div>

    <!-- edit button -->
    <% if edit_href.present? %>
      <div class="sm:self-center sm:ml-auto w-full sm:w-auto">
        <%= link_to edit_href,
                    class: "inline-flex w-full sm:w-auto justify-center items-center rounded-md px-4 py-2 text-sm font-medium
                            bg-[var(--btn-bg)] text-[var(--btn-text)]
                            hover:bg-[var(--btn-hover-bg)] focus:outline-none focus:ring-2 focus:ring-[var(--btn-bg)]" do %>
          Edit profile
        <% end %>
      </div>
    <% end %>
  </div>
</section>
