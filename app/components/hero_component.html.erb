<section class="py-24 text-center">
  <div class="mx-auto max-w-3xl px-6">
    <!-- Headline + text -->
    <h1 class="text-3xl md:text-5xl font-extrabold mb-6">
      Build your <span class="text-[var(--btn-bg)]">identity</span> online
    </h1>
    <p class="max-w-xl mx-auto text-lg text-[var(--muted)] mb-8">
      Whoami helps you showcase your profile, share links, publish posts, and grow your personal brand — all in one place.
    </p>
    <%= link_to new_user_registration_path,
          class: "inline-flex items-center px-6 py-3 rounded-lg bg-[var(--btn-bg)] text-[var(--btn-text)] font-semibold hover:bg-[var(--btn-hover-bg)] transition" do %>
      Create Your Profile
    <% end %>
  </div>

  <!-- Big video below -->
  <div class="mt-12 flex justify-center">
    <%= video_tag "hero.mp4",
          autoplay: true,
          muted: true,
          loop: true,
          playsinline: true,
          class: "w-full max-w-5xl rounded-2xl ring-1 ring-[var(--border)]" %>
  </div>
</section>
